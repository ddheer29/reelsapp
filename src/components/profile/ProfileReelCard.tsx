import { StyleSheet, Text, View } from 'react-native'
import React, { FC } from 'react'

const ProfileReelCard: FC = () => {
  return (
    <View>
      <Text>ProfileReelCard</Text>
    </View>
  )
}

export default ProfileReelCard

const styles = StyleSheet.create({})